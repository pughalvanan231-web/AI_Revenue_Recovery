# Review Requests

*Antigravity will list components here when they are ready for Manus AI to review and evaluate.*

## Current Active Reviews
**Milestone 6: Integration & Idempotency**
- Review `state_store.py` for SQLite atomicity (`INSERT ... ON CONFLICT DO UPDATE`).
- Review `evaluation_harness.py` for pre-diagnosis deduplication logic.
- Review `executor.py` for execution safety constraints.
- Review `llm_agent.py` for `gemini-2.5-flash` real API scaffolding.

**Milestone 5: Probabilistic LLM Layer**
- Review `llm_agent.py` and the updated `evaluation_harness.py`.
- **Constraint Check:** The LLM agent strictly outputs `DiagnosisProposal` and has no capacity to bypass the deterministic policy engine.
- **Cost Efficacy:** Check that the agent's net recovered contribution value beats the simple baseline policy. Rule 3 in the policy engine was updated to abstain (`NO_ACTION`) on low-confidence outputs, protecting against costly false escalations.
- Run `python generate_data.py && python evaluation_harness.py` to see the final simulation results.

**Milestone 4: Evaluation Baseline Harness**
- Review `evaluation_harness.py`.
- **Invariants Check:** Ensure the harness calculates `Net Recovered Contribution Value`, applies cost penalties for retries and friction, and hides the synthetic labels (`is_synthetic_incident`) from the policies.
- Run `python generate_data.py && python evaluation_harness.py` to see the baseline comparisons. Note that the simple deterministic rule baseline outperforms the blind retry in Net Value.

**Milestone 3 Rework: Strict Determinism & Idempotency**
- Review `policy_engine.py`, `state_store.py`, `proposals.py`, and `schema.py`.
- **Idempotency:** Verify that `IdempotencyRepository` successfully prevents duplicate executions based on the `payment_attempt_group_id` key and policy version.
- **Safety:** Verify the `DiagnosisProposal` schema strictly enforces the enum boundaries and confidence bounds (rejecting NaN/Inf).
- **Tests:** Review and run `test_policy_engine.py` using `pytest`. All 10 mandatory security and constraint tests pass.

**Milestone 1 & 2: Schema & Data Generation**
- Review `schema.py` to ensure all fields required by Razorpay are modeled (amount, merchant, issuing bank, device, etc.).
- Review `synthetic_events.jsonl` (generated by `generate_data.py`) to confirm the evaluation baseline data correctly contains normal noise alongside injected degradations (`bank_degradation`, `merchant_checkout_regression`).
- **Goal:** Ensure this schema and data will accurately support evaluating our deterministic and LLM policies.

---
## Completed Reviews
*(None yet)*
